# Co-op Coverage Audit - 2026-07-07

A full sweep of the decompiled game (~437 files) against the mod's sync coverage,
run before the 1.0.29 release. 22 agents: 2 inventory, 10 domain sweeps, 10 adversarial
verifiers. Every confirmed gap below survived an independent refutation attempt.

> **Historical note:** This is a point-in-time audit from before the 1.2.0 box rewrite.
> `Sync/BoxSync.cs`, `Sync/CardBoxSync.cs`, and `Sync/FurnBoxSync.cs` were replaced by
> `Sync/BoxEngine.cs` plus `CardBoxFamily/CardBoxOps`, `FurnitureBoxFamily/FurnitureBoxOps`,
> and `ItemBoxFamily`. File and line references below are historical and may not match
> the current tree.

**Stats:** 18 gap claims -> 10 verified -> 10 confirmed real, 0 refuted; 134 systems confirmed covered.

## Confirmed gaps (fixed in 1.0.29 unless noted)

### [CRITICAL] Auto pack opener (InteractableAutoPackOpener) — join-time save load re-forwards stored packs (cards-packs)

On the guest's join-time world load, the game's own save-restore path InteractableAutoPackOpener.LoadData (decompiled/InteractableAutoPackOpener.cs:370-411) calls AddItem(item, addToFront:true, isPlayer:false) once per pack that was stored in the host's opener at save time. The mod's Harmony prefix ContainerSync.PackOpenerAddItemPrefix (src/CardShopCoop/Sync/ContainerSync.cs:863-885) fires for every one of those AddItem calls because its ONLY guard is `CoopCore.Role != CoopRole.Client || ApplyingRemote` — during ApplyAndLoad the role is already Client (set at CoopCore.cs:389/1154 before the load at CoopCore.cs:2474) and ApplyingRemote is false (this is the game's loader, not a ContainerSync apply). Each firing sends OpPackInsert to the host via SendOp (unconditional Send to connId 1, CoopCore.cs:308/1657 — no in-game/preload gate). The host's HostApplyOp OpPackInsert branch (ContainerSync.cs:361-374) spawns a brand-new pack Item into its own opener UNCONDITIONALLY (the comment even says 'apply unconditionally... dropping it would eat the pack'). The host already had those packs from its own live opener state, so every stored pack is DUPLICATED on the host once per join. ClientPreloadHold only gates the periodic ClientTick snapshot detectors (CoopCore.cs:285/803-804), not this Harmony prefix; MsgType.ContainerOp dispatch (CoopCore.cs:3123-3127) has no 'guest still loading' guard.

**Fix:** Gate PackOpenerAddItemPrefix on `!CoopCore.ClientReloading`, exactly matching the existing CardBoxSync.cs:172 / FurnBoxSync.cs:222 pattern. Change the guard at ContainerSync.cs:865 from `if (CoopCore.Role != CoopRole.Client || ApplyingRemote) return true;` to `if (CoopCore.Role != CoopRole.Client || ApplyingRemote || CoopCore.ClientReloading) return true;` so AddItem calls originating from the game's save-restore during a join are treated as replay, not player inserts. Apply the same guard by symmetry to CleanserAddItemPrefix (ContainerSync.cs:954), whose AddItem is also invoked from the analogous cleanser LoadData restore path. Alternatively (broader fix) wrap the guest's world load so all interactable LoadData mutations are recognized as replay, but the per-prefix ClientReloading guard is the minimal change consistent with the codebase's existing approach.

### [CRITICAL] Sell / relocate an EXISTING placed shelf or counter via box-up (guest) (layout)

A guest can box up an existing HOST-placed furniture object (StartMoveObject -> BoxUpObject creates a new InteractablePackagingBox_Shelf in the guest's RestockManager.m_ShelfPackagingBoxList) and then SELL it (InteractionPlayerController.ConfirmSellFurniture: fires CEventPlayer_AddCoin(price/2) then box.OnDestroyed). The AddCoin is forwarded to the shared wallet via DayEndBlockPrefix->EconContrib, crediting the shared bank. But the box the guest created is guest-only; the host never boxed that object. FurnBoxSync.HostApplyRemoved -> FindBox looks in the HOST's m_ShelfPackagingBoxList and finds no match (returns null) -> removal ignored. The host's shelf survives and its next PopState re-spawns it on the guest. Net: shared wallet gains the sell price, the furniture is NOT removed.

**Fix:** Patch InteractionPlayerController.ConfirmSellFurniture on the CLIENT: block-before-charge (prefix returns false locally so no local AddCoin and no local box.OnDestroyed) and instead forward a 'sell existing furniture' op identifying the boxed object by (kind,index)/identity + type, which the host validates against a REAL placed object it owns, then the host performs the single AddCoin AND the object's OnDestroyed so PopulationSync mirrors the removal to everyone. Additionally, forward the initial BoxUpObject(holdBox:true) of an existing placed object (patch InteractableObject.BoxUpObject on the client, distinguishing a placed object being boxed from a delivery unpack) so relocate-via-box mirrors symmetrically and the host's own copy is boxed/held in step, closing the same class of gap for relocation as well as selling.

### [MODERATE] Restock / furniture / license purchases (guest-forwarded) (economy)

For a GUEST purchase, the coin charge and the product are two DECOUPLED forwarded messages: the guest fires CEventPlayer_ReduceCoin (forwarded as EconContrib kind 2, which the host affordability-guards and can DECLINE) and, separately, the product order/furniture/license (ForwardOrder / ForwardFurniture / ForwardLicense, which the host applies UNCONDITIONALLY with no coin check). When the host declines the charge because the shared wallet is short, it still delivers the product for free. This is structural, not a rare race: the guest's local wallet mirror does NOT decrement on its own forwarded ReduceCoin (DayEndBlockPrefix returns false without applying locally, GamePatches.cs:398-402), so it only refreshes on the ~0.5s CoinSet echo. A guest making several rapid purchases all pass their own local affordability check against the same stale balance; the host's _pendingReduceThisFrame guard (CoopCore.cs:3419-3427) then correctly DECLINES the ones that overrun the real wallet — but the corresponding forwarded products (OrderRequest CoopCore.cs:2912, FurnitureOrder CoopCore.cs:2853, LicenseUnlock CoopCore.cs:2988) are applied regardless. Restock and furniture ALSO fire a forwarded AddShopExp, so the guest even gains XP for the unpaid item.

**Fix:** Adopt the grading/hire/unlock coupling for the three paths. Preferred: block the guest's local purchase BEFORE its ReduceCoin (prefix that returns false like StaffSync/ShopStateSync do) and forward a single op whose host handler re-checks affordability from CPlayerData.m_CoinAmountDouble and, only if it passes, atomically issues the host-side ReduceCoin AND delivers the product+XP. Minimal alternative without restructuring the wire protocol: in the EconContrib kind-2 decline branch (CoopCore.cs:3420-3424), record the declined amount per ConnId for this dispatch frame, and have the OrderRequest/FurnitureOrder/LicenseUnlock handlers, when they see a matching just-declined charge from the same peer this frame, refuse+toast instead of delivering (or, since the handlers already know the authoritative price — lineCost/fp.price/license price — refund the product's cost the way the catalog-miss branch already does).

### [MODERATE] Decoration objects (posters / statues / arrow-signs / vertical plants) - guest placement (layout)

A guest placing a deco OBJECT (EDecoObject, category-3 deco: 72 posters, 35 statues, 13 arrow signs, 8 vertical/wall plants) has no forward path. PlaceDecoUIScreen.StartPlaceDecoItem spawns the object into the guest's m_DecoObjectList (kind 5) and does AddDecoItemToInventory(-1) purely locally. PopulationSync is host-authoritative (host broadcasts its kind-5 roster, client reconciles TO it), so the guest's new deco is an 'extra beyond host roster' and gets destroyed by the client's next PopState reconcile. Meanwhile the guest already consumed its inventory count. Worse: ObjMoveSync.Tick runs on the client and will send an ObjMoveRequest keyed (kind5<<24|index) for the guest's settled deco; on the host ObjMoveSync.Resolve/ApplyRemote does an index lookup with NO type check (Resolve at ObjMoveSync.cs:189-196) and SetPositionAndRotation, so it teleports whatever DIFFERENT host deco happens to sit at that kind-5 index.

**Fix:** Minimal, matching the mod's existing block idiom (RenamerBlockPrefix / tournament blocks): add a client prefix on `PlaceDecoUIScreen.StartPlaceDecoItem` (and optionally `ShopBuyDecoUIScreen.OnPressBuyShopDecoItem`) that, when Role==Client, shows a register/toast line ('deco placement is host-only') and returns false BEFORE `AddDecoItemToInventory(-1)` runs, so the guest neither loses inventory nor spawns an orphan kind-5 object. Better (parity with furniture): forward StartPlaceDecoItem to the host (spawn EDecoObject on host via SpawnDecoObject, let PopulationSync mirror it back) and mirror m_DecoItemInventoryList deltas + include it in the join save. Independently, harden ObjMoveSync.Resolve/ApplyRemote with a per-kind type guard so a kind-5 request whose resolved object's m_DecoObjectType doesn't match the expected type is dropped (defense-in-depth against the stray-move teleport).

### [MODERATE] Smelly customers (handheld deodorant spray) (customers)

When the GUEST picks up a deodorant spray and sprays it at a smelly customer, nothing happens to the host's real smelly customer. InteractionPlayerController's hold-spray path (decompiled/InteractionPlayerController.cs:1628-1631) calls CustomerManager.GetCustomerList()[i].DeodorantSprayCheck(...) against the LOCAL customer list. On the guest that list is nothing but the suppressed/deactivated puppet customers (NpcSweepTick disables them, ClientBlockPrefix stops their sim) - Customer.DeodorantSprayCheck (decompiled/Customer.cs:532-558) reduces m_SmellyMeter and clears m_IsSmelly on that local puppet object, but the host's authoritative smelly customer (the one whose stink cloud the guest actually sees, mirrored via NpcSync's Smelly flag) is a DIFFERENT object on the host and is never touched. There is no MsgType or forward for a handheld spray hit.

**Fix:** Mirror the vanilla station model for the handheld. Add a client-side Harmony postfix (or transpiler guard) on InteractionPlayerController.RaycastHoldSprayState: on the guest, suppress the local GetCustomerList() DeodorantSprayCheck loop (it only hits inert puppets) and instead, when the spray VFX fires with content, forward a lightweight op to the host carrying the spray world position + range (2.5) + potency (1). Add a MsgType (e.g. HandheldSprayHit) and a dispatch case in CoopCore that, host-side, runs DeodorantSprayCheck against the host's REAL customer list at that position (the same call InteractableAutoCleanser.Spray uses); the existing NpcSync Smelly flag + ReportSync.smellyCustomerCleaned mirror then echoes the cleaned result back to the guest automatically. If handheld co-op spraying is judged low-value, the cheaper mitigation is to gate it on the client: block entering HoldSprayState with a toast ("the host or the auto-cleanser clears smelly customers") so the guest never wastes deodorant content on a guaranteed no-op.

### [MODERATE] Worker AI vs guest-carried delivery boxes (BoxSync interplay) (workers)

On the host, a box the GUEST is carrying is marked _remoteCarried and its host-side GameObject is SetActive(false), but the host never sets m_IsBeingHold or m_PreventWorkerTakeBox on that box. Worker.DetermineShopAction (RestockShelf/RefillCleanser/RefillCardOpener) builds its candidate list via RestockManager.GetItemPackagingBoxListWithItem, which filters on IsValidObject() && GetItemCount()>0. InteractableObject.IsValidObject() returns !m_IsBeingHold (decompiled/InteractableObject.cs:834-841) and does NOT check gameObject.activeSelf, and CanWorkerTakeBox() returns !m_PreventWorkerTakeBox. So a host worker still treats the guest-held box as a valid, takeable restock source.

**Fix:** On the host, exclude _remoteCarried boxes from worker consideration for the duration of the guest carry. When a box enters _remoteCarried (BoxSync.cs HostApplyRequest ~541 and the HostTick re-assert ~499), set m_PreventWorkerTakeBox=true (and/or m_IsBeingHold=true) on the host box via reflection so CanWorkerTakeBox()/IsValidObject() reject it; clear the flag when the guest releases it (_remoteCarried.Remove path ~542 and on set-down). Setting m_PreventWorkerTakeBox is the cleaner choice since m_IsBeingHold interacts with other host code paths. This mirrors the existing IsBeingHeld guard but for the guest-carry direction.

### [MODERATE] Save/persistence - manual Load Game from pause menu (guest) (time-save)

The mod blocks WRITES via SaveGuardPrefix on CGameManager.SaveGameData, but does NOT block a guest from LOADING a different save mid-session. PauseScreen.OnPressLoadGame -> SaveLoadGameSlotSelectScreen.OpenScreen(isSaveState:false) -> OnPressLoadGame -> CGameManager.LoadMainLevelAsync("Start", slot) loads the guest's OWN save into the game 'Start' scene. This is a scene change to a non-Title scene, so the mod's OnSceneLoaded Shutdown branch (which only fires on scene.name=="Title") never runs, and ClientReloading is NOT set (that flag is set only by the mod's own join reload at CoopCore.cs:2472). The guest stays Role=Client with the TCP/Steam socket still open; the host is never told the guest left.

**Fix:** Broaden CoopCore.OnSceneLoaded (CoopCore.cs:754): if Role==Client && !ClientReloading && a fresh game-level ("Start") scene loads, treat it as the guest leaving and call Shutdown("left the session")/Disconnect — this catches manual Load Game plus any other non-mod scene reload (e.g. Back To Menu), and is safer than only patching the button. Apply the equivalent guard for the host so guests aren't stranded. (An additional client/GuestBorrowedWorld prefix on SaveLoadGameSlotSelectScreen.OpenScreen or OnPressLoadGame that blocks the load with a "Leave the co-op session first" toast is a good complementary UX guard, but the OnSceneLoaded broadening is the load-bearing fix.)

### [MODERATE] Loose boxes / warehouse restock (BoxSync type-sanity guard) (restock-storage)

BoxSync.HostApplyRequest rejects any guest box-edit whose reported item type differs from the host box's CURRENT compartment type (`if ((int)box.m_ItemCompartment.GetItemType() != e.Type) continue;`, BoxSync.cs:535). The guest can legitimately CHANGE an open box's item type via InteractablePackagingBox_Item.RemoveItemFromShelf (decompiled line 280-297): right-clicking a stocked display/warehouse shelf while holding an EMPTY OPEN box makes the box's compartment adopt the shelf's item type (ShelfCompartment.CheckItemType sets the type when m_ItemAmount<=0, line 242-247) and pulls one item off the shelf into the box. On the guest this is two mutations: the shelf compartment loses 1 item (WorldSync, kind 0/3 - syncs to host fine) and the box gains 1 item of the newly-adopted type (BoxSync - REJECTED by the guard, because the host's mirror of that box is still type None). The shelf decrement reaches the host but the box gain never does, so on the host-authoritative world the item is destroyed. Within ~6s the host's next BoxState broadcast (box still None/0) overwrites the guest's _locallyTouched copy and the item disappears on the guest too.

**Fix:** In BoxSync.HostApplyRequest (BoxSync.cs:535), relax the type guard to only reject when BOTH the host box and the requested entry are non-None and disagree: e.g. `int htype=(int)box.m_ItemCompartment.GetItemType(); if (htype != e.Type && htype != (int)EItemType.None && box.m_ItemCompartment.GetItemCount() > 0) continue;` — allowing an empty/None host box to adopt the guest's reported type. Then let ApplyToBox(box, e) run: for an empty->typed transition it must set the compartment's item type before spawning count (mirror FillBoxWithItem: SetCompartmentItemType + CalculatePositionList, as ApplyClosedCount already does for the pos-list case). This makes the reclaimed item land on the host box so conservation holds. Alternatively, patch RemoveItemFromShelf on the client to forward an explicit reclaim op the host replays authoritatively (heavier, but avoids widening the guard for genuinely-mangled requests).

### [MODERATE] Grading — re-submit of an already-graded card (re-grade) (grading-mail)

When the guest re-submits a card that is ALREADY graded (selected from the graded album), the host removes that graded card from m_GradedCardInventoryList TWICE: once via the GradedRemove mirror (RemoveGradedCardPostfix -> ForwardGradedRemoval -> host ApplyCardDelta -> RemoveGradedCard) and again via GradingSync.HostApplyOp's own degrade loop (CoopCore/Sync/GradingSync.cs lines 425-432, unconditional RemoveGradedCard for any cardGrade>0). RemoveGradedCard(ignoreGradedCardIndex:true) matches purely by identity (cardSaveIndex+grade+expansion+isDestiny), so if the host owns >=2 identical graded copies, the second (uninvolved) copy is also destroyed. The line-429 RemoveGradedCard also runs WITHOUT GamePatches.ApplyingRemoteCards set, so its own postfix re-broadcasts a GradedRemove to all guests, making the same double-removal happen on any guest that owns a duplicate too.

**Fix:** Delete the degrade loop at GradingSync.cs lines 425-432. The RemoveGradedCardPostfix mirror (the 1.0.22 mirror the code documents at 314-315) already removes the graded card on the host and relays it to all guests at SELECTION time, exactly like every other RemoveGradedCard, so the loop is redundant. Also update the stale comments at lines 26-28 and 421-424 which still claim the selection removal 'is a direct list edit no mirror carries'. If a belt-and-suspenders removal is preferred over deletion, gate the loop on HasGradedCardInAlbum AND wrap it in GamePatches.ApplyingRemoteCards=true (try/finally) so it neither removes a copy the mirror already took nor re-broadcasts a second GradedRemove — but deletion is the simplest correct fix.

### [MODERATE] Smelly customers / hand-held deodorant spray (catch-all)

The guest cannot clean smelly customers with the hand-held Deodorant spray item. InteractionPlayerController.RaycastHoldSprayState (decompiled line 1612-1631) sprays by iterating CustomerManager.GetCustomerList() and calling Customer.DeodorantSprayCheck on each. On the guest, real customer sim is suppressed (CustomerManager.Update ClientBlockPrefix, NpcSweepTick deactivates stray NPCs) and the visible customers are NpcSync puppets stored in a private _puppets dictionary with their Customer MonoBehaviour DestroyImmediate'd during the strip (NpcSync.Spawn, src/CardShopCoop/Sync/NpcSync.cs ~line 579-587) — they are NOT in GetCustomerList(). So the guest's spray loop iterates an empty/inert list. There is no SprayRequest MsgType and no guest->host forward for spraying (confirmed: grep for Spray/Deodorant/SmellyMeter across CoopCore.cs, Net/Msg.cs, Patches/GamePatches.cs returns nothing). NpcSync DOES mirror the Smelly flag host->guest (NpcSync.cs line 125/460/563), so the guest SEES the smelly FX but cannot act on it. Nothing blocks the guest from entering HoldSprayMode either, so the can depletes locally (DepleteContent, line 1626) for zero effect.

**Fix:** Add a guest->host forwarded spray op. Patch the client-side spray fire in InteractionPlayerController.RaycastHoldSprayState so the guest still plays local VFX and depletes the can for feedback, but instead of iterating its own empty GetCustomerList() it forwards a SprayRequest carrying the spray world-position, range (2.5f), and potency (1). Add the SprayRequest MsgType to Net/Msg.cs and a dispatch case in CoopCore that runs the DeodorantSprayCheck loop over the HOST's real GetCustomerList(), so the authoritative m_SmellyMeter decrements and the customer actually gets cleaned; the cleaned state then flows back to the guest via the existing NpcSync Smelly display flag. This mirrors the existing host-authoritative pattern already used for the AutoCleanser (which cleans host-side and mirrors the flag back).

## Deferred backlog (minor / cosmetic - NOT fixed yet)

- **[minor] Product licenses (catalog mismatch, no refund)** - When a guest buys a license for a product NOT present in the host's catalog, the guest's forwarded ReduceCoin already debited the shared wallet, but the host's LicenseUnlock handler, when ApplyLicenseUnlock returns false (ResolveRestockIndex miss, CoopCore.cs:1388-1392), only sends a 'couldn't unlock (product missing)' toast and does NOT refund. The parallel OrderRequest and FurnitureOrder paths DO refund on a catalog miss (CoopCore.cs:2940 AddCoin(cost); 2878/2895 AddCoin(refund)); the license path is the inconsistent one. Catalog divergence from differing content packs is an explicitly knownAccepted scenario for orders, so it is reachable here too.
- **[minor] Scanner-restock unlock (warehouse upgrade)** - The scanner-restock unlock purchase (ScannerRestockScreen.OnPress... line ~111: sets CPlayerData.m_IsScannerRestockUnlocked=true and fires ReduceCoin(m_UnlockCost)) is NOT patched by the mod. On the guest it charges the shared wallet (ReduceCoin forwarded, real host debit) and sets the guest's LOCAL flag true, but the unlock is never forwarded to the host. The scanner flag is only ever pushed HOST->client inside LicenseState (CoopCore.cs:2250 reads the HOST's flag; guest applies it OR-merged at 3036 CPlayerData.m_IsScannerRestockUnlocked |= scanner). Because the merge is one-directional (host's value into guest) and OR-only, the guest's self-unlock can never propagate to the host, and since guest saves are blocked the unlock evaporates on rejoin.
- **[minor] Deco-object purchase (buy into decoration inventory)** - A guest buying a deco object (ShopBuyDecoUIScreen.OnPressBuyShopDecoItem, category 3) fires CEventPlayer_ReduceCoin(price) - forwarded to the shared wallet - and AddDecoItemToInventory(+1) purely locally on the guest. m_DecorationInventoryList is a CPlayerData static that no sync module mirrors, so the host's inventory count never increments. The shared wallet paid but only the guest's mirrored inventory shows the item.
- **[minor] Customers (purchase interest from nearby pack-opening)** - When the GUEST opens a card pack near a host customer, the host's customers get no buying-interest boost. Vanilla: CustomerManager.PlayerFinishOpenCardPack (decompiled/CustomerManager.cs:706-715) walks the real customer list and calls AddPlayerOpenPackNearby() on every customer within 3m of the LOCAL player, nudging their buy behavior. The guest's pack-open is broadcast only as MsgType.Activity, which CoopCore handles (CoopCore.cs:2681-2690) as a purely cosmetic avatar tag + ShowPackOpen animation - it never calls PlayerFinishOpenCardPack on the host, and the host has no knowledge of which of its customers the guest is standing near (the guest is a relayed avatar, not a position the customer AI reads).
- **[minor] Checkout / register serving** - The guest can still click a physical cashier counter and enter the game's native cash-counter mode (InteractableCashierCounter.OnMouseButtonUp is never blocked on the client). On the guest the counter's m_CurrentCustomer is null (customer sim is host-only and swept inactive), so there is nothing to scan or pay, but OnMouseButtonUp sets the guest's LOCAL m_IsMannedByPlayer=true and calls InteractionPlayerController.SetPlayerPos(m_LockPlayerPos.position) + OnEnterCashCounterMode, snapping the guest's camera to the empty register and locking movement until they press a move key / ESC (OnPressEsc). Nothing is forwarded, no money moves, and it is self-recoverable, so it is a UX wart, not a desync.
- **[minor] Checkout report statistics** - When the GUEST completes a sale through RegisterServe, the host runs OnPressSpaceBar / EvaluateCreditCard with m_IsMannedByPlayer=false (the host player is not standing at that counter). The money (CEventPlayer_AddCoin), the PriceChangeManager transactions (ItemSold/CardSold), XP and customer review all fire correctly regardless of manned-by-player. BUT the manual-checkout report counters and the manual-checkout achievement are gated behind `if (m_IsMannedByPlayer)`: m_GameReportDataCollect.manualCheckoutCount++ / Permanent.manualCheckoutCount++ and AchievementManager.OnCustomerFinishCheckout are skipped for guest-completed sales, and CheckoutCustomer tutorial credit is skipped.
- **[cosmetic] Save/persistence - manual Save Game slot screen (guest cosmetic)** - Guest can open Pause -> Save Game -> SaveLoadGameSlotSelectScreen(isSaveState:true) and press a slot. OnPressSaveGame -> CGameManager.SaveGameData(slot) is correctly blocked by SaveGuardPrefix so nothing is written, BUT the UI still shows the save-slot screen and (for a slot whose name != PlayerName) can pop the 'overwrite' confirm dialog, giving the guest the false impression a save happened / letting them poke an overwrite prompt that silently no-ops.
- **[minor] Empty-box take from storage (BoxSync/ContainerSync hand-off)** - When the guest clicks the empty-box storage to TAKE a box, ContainerSync.TakeBoxPrefix blocks the local spawn and forwards a take op; the host spawns the empty box via RestockManager.SpawnPackageBoxItem and places it on the FLOOR at the storage's m_EmptyBoxSpawnLoc (ContainerSync.cs HostApplyBoxTake, 499-503), then BoxSync mirrors it to the guest as a loose floor box. The guest is never put into hold-box mode, so from the guest's view they clicked 'take box' and a box appears on the ground next to the dispenser instead of in their hands.

## Confirmed covered (134)

- [economy] Wallet mutation is fully event-driven: m_CoinAmount/m_CoinAmountDouble are only ever written by CEventPlayer_AddCoin/ReduceCoin/SetCoin handlers (CPlayerData.cs:2036-2079); no direct writes bypass the events, so intercepting the events (GamePatches.DayEndBlockPrefix 393-402) captures every guest-side coin mutation
- [economy] Guest-earned/spent coin forwarded to host and mirrored back: DayEndBlockPrefix forwards AddCoin/ReduceCoin/AddShopExp/AddFame as EconContrib (GamePatches.cs:393-412); host applies (CoopCore.cs:3401-3434) with a per-frame affordability guard on ReduceCoin (_pendingReduceThisFrame, 3419-3427) that prevents the shared wallet going negative from stale-mirror over-spend; HostTick broadcasts authoritative wallet via CoinSet with 0.5s change-gate + 15s heal (2082-2097); guest mirrors via CEventPlayer_SetCoin (2590-2605)
- [economy] Shop XP / level / fame progression mirrored: HostTick broadcasts ProgressSet (exp/level/fame) with change-gate + 15s heal (2099-2109); guest applies level-first then SetShopExp/SetFame with a guarded single ShopLeveledUp (2655-2673) avoiding spurious re-level; level-up side effects (customer count max in CustomerManager.OnShopLeveledUp) are host-only sim and harmless on the guest
- [economy] Register/checkout serving credits money+XP+fame in the host's REAL simulation: RegisterServe.Serve drives the vanilla counter methods host-side (OnMouseButtonUp scan, credit-card/cash completion, RegisterServe.cs:63-181) so all sale AddCoin/AddShopExp/AddFame fire once on the host; no double-count, guest only sends ServeRequest
- [economy] Restock ordering charge+delivery: guest's EvaluateCartCheckout ReduceCoin is forwarded (EconContrib) and each SpawnPackageBoxItemMultipleFrame is caught by OrderPrefix->ForwardOrder (GamePatches.cs:187-192); host spawns without re-charging (relies on the forwarded charge) and refunds on catalog miss (CoopCore.cs:2932-2941); identity travels as itemType+isBig+name FNV not raw index
- [economy] Furniture purchase charge+delivery: FurnitureOrderPrefix forwards ForwardFurniture (GamePatches.cs:175-185); host spawns with a catalog pre-check and refunds on miss/spawn-fail (CoopCore.cs:2871-2901); DisablePreviewGuardPrefix prevents the NRE that used to eat the money
- [economy] Product licenses shared: SetUnlockItemLicense postfix forwards LicenseUnlock by identity (GamePatches.cs:168-173); host ApplyLicenseUnlock + achievements/tutorial credit + echo to other clients (CoopCore.cs:2988-3011); host HostTick LicenseState heal (10s/60s over the EPL virtual catalog) keeps the guest converged (2240-2270)
- [economy] Bills (rent/electric/salary): guest pay-buttons blocked before charge and forwarded as ShopOp (ShopStateSync PayRent/Electric/Salary/AllPrefix 184-196); host runs the vanilla pay path (the single charge) via HostPayBill (338-355); BillAccrualPrefix blocks the guest's local day-start accrual/auto-pay (198-201) so accrual is host-authoritative; PhoneManager.OnDayStarted->EvaluateNewDayBill is the blocked path
- [economy] Shop expansion (rooms / warehouse rooms / lot-B): RoomCheckoutPrefix/UnlockShopBPrefix block before charge and forward ShopOp (ShopStateSync 203-218); HostUnlock re-checks level+affordability from HOST state and does the single ReduceCoin+unlock+AddShopExp atomically (357-419), so a stale guest UI can neither underpay nor double-buy
- [economy] Staff hire fee: HirePrefix blocks the guest before its ReduceCoin and forwards StaffOp (StaffSync 131-172); HostHire re-checks affordability and charges once host-side (230-240). Worker fire/give-bonus/task-assignment (WorkerInteractUIScreen, ReduceCoin at 176) are unreachable on the guest — worker puppets are stripped NpcSync clones with all MonoBehaviours+colliders DestroyImmediate'd (NpcSync.cs:586-596), so Worker.OnMousePress (which opens WorkerInteractUIScreen) can't fire; host-only by design
- [economy] Card grading fee: SubmitPrefix blocks the guest before its ReduceCoin and forwards GradingOp; host HostApplyOp re-checks affordability (GradingSync.cs:414) and does the single ReduceCoin(total) coupled with enroll (438) — the CORRECT charge+product coupling; RestockManager.OnDayStarted grading maturation blocked on guest (MatureBlockPrefix, pure-maturation verified)
- [economy] Shop deco STYLE purchase (wall/floor/ceiling): BuyDecoPrefix blocks OnPressBuyShopDeco before charge and forwards SettingsOp; HostBuyDeco re-checks owned+affordability and charges once (SettingsSync.cs:215-243); ownership bool lists mirrored via SettingsState
- [economy] PriceChangeManager.AddTransaction is a no-op stub in this build (decompiled PriceChangeManager.cs:340-342) — the transaction log / statistics list never populates, so there is no transaction-history divergence to sync; m_TransactionDataList is effectively dead
- [economy] Day-rollover money events are host-only: guest OnDayEnded blocked and OnDayStarted allowed exactly once (DayEndBlockPrefix 382-387); the single allowed OnDayStarted's money-bearing listeners are all separately blocked — PhoneManager->EvaluateNewDayBill (BillAccrualPrefix), RestockManager grading maturation (MatureBlockPrefix), PriceChangeManager reroll (MarketSync ClientBlockPrefix), card-base rolls (GenerateCardMarketPriceBlockPrefix)
- [economy] Pack-opening XP: CardOpeningSequence fires AddShopExp (decompiled:558) which is forwarded via EconContrib kind 3 and banked host-side — guest earns progression for opening packs correctly
- [economy] No 'cheat'/debug coin path is exposed to the guest: m_CanRunDebugString/m_DebugString are set during restock but there is no guest-reachable coin-cheat event; the coin floor auto-reset (CPlayerData.cs:2048-2052 at < -100,000,000) only fires via SetCoin which the sync itself uses
- [cards-packs] Manual pack rip (CardOpeningSequence.OpenScreen, decompiled line 641-773): each side rolls its own contents with UnityEngine.Random (on-screen card faces diverge harmlessly = cosmetic), but every card is banked via CPlayerData.AddCard (line 723) which is forwarded verbatim as CardDelta by GamePatches.AddCardPostfix (GamePatches.cs:285-306). The collection stays identical on both sides; the reveal animation is local. XP from the open (CEventPlayer_AddShopExp, line 558) forwards via DayEndBlockPrefix->EconContrib. GamePatches.cs:281 comment confirms the AddCard-during-OpenScreen path is deliberately kept crash-safe.
- [cards-packs] Auto pack opener normal operation: host runs the machine's own Update()/OpenPack (rolls all contents host-side via CardOpeningSequence.GetPackContentCardDataList); the guest's local m_StoredItemList is drained in ContainerSync.ApplyPackMirrorToMachine (ContainerSync.cs:642-681) so its Update() never rolls packs the host never rolled. Guest button click forwards OpPackTurnOn/OpPackCollect; the collector's reveal (ShowCardObtainedPage.ShowCardObtained -> AddCard, decompiled line 80) carries cards into the shared binder via CardDelta, and HostApplyPackCollect (ContainerSync.cs:451-490) banks only the DIFFERENCE to avoid double-counting. Full mirror of stored count/processing/timer/opened/output in KindPackOpener state (ContainerSync.cs:292-305, 545-571).
- [cards-packs] Guest inserting a pack into the auto-opener does not duplicate mid-session: PackOpenerAddItemPrefix forwards OpPackInsert AND the source delivery box's item-count decrement is caught by BoxSync.Differs (BoxSync.cs:219-227 includes Count) and applied host-side via HostApplyRequest->ApplyToBox->ApplyClosedCount (BoxSync.cs:528-543), so the host removes the pack from its box copy while adding it to its opener — no net duplication for the live-play path (distinct from the join-load path flagged as a gap).
- [cards-packs] Card display shelves + glass/combi counters + tournament prize shelves: CardShelfSync (src/CardShopCoop/Sync/CardShelfSync.cs) mirrors single-card display slots (kinds 2 CardShelf, 3 CardItemCombiShelf, 14 TournamentPrizeShelf) via snapshot diff with full CardData identity (monster/expansion/border/grade/gradedIdx/foil/destiny/champion). A customer buying a displayed card on the host runs ReduceCard (forwarded as CardDelta) and the slot-empty transition mirrors; ClearSlot also fixes the stuck price-tag. Placement replays the game's SpawnInteractableObject/SetCardOnShelf recipe.
- [cards-packs] Ghost / destiny / dimension / champion / foil / border / graded card variants all travel intact: Msg.WriteCard/ReadCard (Net/Msg.cs:147-174) serialize expansionType, monsterType, borderType, isFoil, isDestiny, isChampionCard, cardGrade, gradedCardIndex — every field on CardData (decompiled/CardData.cs). 'Ghost' and 'destiny' are encoded via expansionType + isDestiny (Ghost packs randomize isDestiny, CardOpeningSequence line 1225-1229); there is no separate 'dimension' field. So all pull variants reach the other side correctly through CardDelta / CardShelfDelta / CardPriceSet.
- [cards-packs] Card storage shelves and bulk donation boxes (deck-box-style card containers): store their own compact card inventory (separate from the collection) mirrored by ContainerSync KindCardStorage/KindDonation (SetCompactCardDataAmountList postfixes forward, ClientApplyState applies). Moving cards in/out also runs collection AddCard/ReduceCard which forwards as CardDelta. TakeItemToHand is blocked on the client (ContainerSync.cs:804/890-895) so the guest can't mint a phantom.
- [cards-packs] Workbench / quick-fill collection consumption: WorkbenchUIScreen (decompiled line 378) removes cards via CPlayerData.ReduceCardUsingIndex, which is patched by GamePatches.ReduceCardIndexPostfix (GamePatches.cs:148-158) and forwarded as a CardDelta removal — covers collection mutations that bypass ReduceCard.
- [cards-packs] Graded-card album removals: CPlayerData.RemoveGradedCard (decompiled line 1552) forwarded by GamePatches.RemoveGradedCardPostfix (GamePatches.cs:320-326) as GradedRemove — covers the one collection path (graded inventory list) that ReduceCard never touches.
- [cards-packs] Play-table card placement: verified pure visuals. PlayTableSync (src/CardShopCoop/Sync/PlayTableSync.cs) mirrors only per-seat TableGameItemSet meshes (playmat/deckbox/comic EItemTypes) host->client; decompiled InteractablePlayTable.cs / PlayCardGameManager.cs contain NO AddCard/ReduceCard/SetCard — the customer match and the player-vs-player minigame never lay real CardData on the table or mutate the shared collection, so no card sync is needed (the flagged 'PvP deck play' inventory item requires no card-collection sync).
- [cards-packs] Champion/special-event direct adds are not a live path: CPlayerData.AddChampionCard / AddSpecialEventCard / SetCard (decompiled lines 1641-1742) have ZERO callers anywhere in the decompiled game set, so they can't mutate the shared collection during normal 2-player play (nothing to forward).
- [layout] Shelf/counter/furniture MOVE + ROTATE by guest: ObjMoveSync settle-gated pose diff -> ObjMoveRequest (client) / ObjMoveDelta (host authoritative); host dropIfHostMoving guard prevents the snap-back echo war (CoopCore 3304-3318, ObjMoveSync.cs). Price-tag canvas group (m_Shelf_WorldUIGrp) and auto-pack-opener world UI re-anchored on remote apply.
- [layout] Placed-object POPULATION (all 15 ShelfManager kinds incl. decorations kind 5): PopulationSync host 3s roster + client reconcile via game's own SpawnInteractableObject/SpawnDecoObject self-registration and OnDestroyed; kind-5 hashed/serialized on m_DecoObjectType not the -1 m_ObjectType so deco add/remove re-broadcasts and spawns correctly (PopulationSync.cs). Host-side decoration is fully mirrored to guest.
- [layout] Furniture PURCHASE by guest: GamePatches.FurnitureOrderPrefix blocks local phantom + ForwardFurniture; host spawns officially via SpawnInteractableObjectInPackageBox with catalog pre-check + refund+Toast on miss (CoopCore 2853-2903); DisablePreviewGuardPrefix NRE guard so the programmatic spawn completes (GamePatches 223-234).
- [layout] Furniture DELIVERY BOX unpack + place + sell/trash by guest: FurnBoxSync PlacePrefix forwards the unpack CONFIRM (host runs real PlaceMovedObject), DestroyedPrefix->OnLocalDestroyed forwards sell/trash of a DELIVERY box, snapshot mirror with carry transitions + stale-echo guards + rigidbody wake to avoid frozen floating boxes (FurnBoxSync.cs).
- [layout] Wall / floor / ceiling MATERIAL customization: SettingsSync BuyDecoPrefix (block-before-charge, host charges once via HostBuyDeco) + EquipDecoPostfix forward; ClientApplyState applies deco ownership bool lists + 6 equipped indices via ShopCustomizationManager.ChangeWall/Floor/CeilingMaterial, both shop-lot A and B (SettingsSync.cs).
- [layout] OPEN/CLOSED sign + warehouse-entry sign: ShopStateSync OpenSignPrefix/WarehouseSignPrefix block-and-forward, host runs the real OnMouseButtonUp (tutorial gate + swap debounce), ShopState broadcast sets m_IsShopOpen/m_IsWarehouseDoorClosed + re-evaluates sign meshes on the client (ShopStateSync.cs 220-241, 526-538).
- [layout] Shop LIGHT switch: LightSwitchPrefix block-and-forward -> host LightManager.ToggleShopLight -> flipped m_IsShopLightOn ships via LightState broadcast (ShopStateSync 227-234, 332-336).
- [layout] Shop room / warehouse-room / lot-B EXPANSION by guest: ShopStateSync RoomCheckoutPrefix/UnlockShopBPrefix block-before-charge -> ShopOp; host HostUnlock recomputes cost/eligibility from HOST state (no underpay/double-buy), charges once, runs StartUnlockNextRoom/StartUnlockNextWarehouseRoom/SetUnlockWarehouseRoom; ShopState broadcast drives the client's incremental unlocks + idempotent UnlockRoomManager.Init() wall repaint heal (ShopStateSync 203-218, 357-419, 494-522).
- [layout] Guest standing in a newly-unlocked room: LockedRoomBlocker.HideBlocker is a wall/animation hide (front-row uses m_Building exterior), not a player-trapping collider; the same hide runs on the guest via the mirrored StartUnlockNextRoom, and any deco/furniture snapped to that wall auto-boxes/returns via OnLockedRoomHidden on both sides. No trap or crash (UnlockRoomManager.cs, LockedRoomBlocker.cs).
- [layout] Bills (rent/electric/salary) accrual + payment: BillAccrualPrefix blocks client-side accrual (host authoritative), pay-bill prefixes block-before-charge + forward ShopOp, host runs vanilla pay handlers, ShopState mirrors bill set + phone badge (ShopStateSync).
- [layout] Cashier-counter checkout/trade toggles + tournament play-table numbers: SettingsSync CashierPostfix/TableNumberPostfix forward, host applies via the interactable setters, SettingsState mirrors (SettingsSync 167-201, 510-533). Counter world-screens re-shown on headless furniture placement (FurnBoxSync PlaceFromBox).
- [layout] Nightly game-event format/expansion/fee (play-table event): SettingsSync GameEventPostfix/GameEventFeePostfix forward pending format+expansion and per-format fee; SettingsState mirrors current+pending+fee list.
- [layout] Shop NAME: RenamerBlockPrefix blocks the rename screen on the guest with a toast (host-only, host->client ShopName broadcast) - matches knownAccepted host-only-decision philosophy (GamePatches 236-245).
- [customers] Customer lifecycle (spawn/despawn/entry timers/max count): host-only sim, guest fully suppressed - GamePatches ClientBlockPrefix on CustomerManager.Update and Customer.ActivateCustomer, plus CoopCore.NpcSweepTick (CoopCore.cs:893-921) deactivates any stray local customers on the client every tick
- [customers] Customer puppets & rendering (position/yaw/anim/appearance): NpcSync host streams 8Hz batched puppet states (NpcSync.HostCollect), client renders stripped-clone interp puppets (ApplyBatch/TickPuppets); names via CharacterCustomization, gender via name prefix / Female flag
- [customers] Customer purchase decisions & item pickup off item shelves: host-only sim (TakeItemFromShelf runs only on host); the guest's shelves visually deplete via WorldSync (Sync/WorldSync.cs) 0.75s host-authoritative snapshot diff by (shelfKind,shelfIdx,compIdx), applied as atomic clear-and-rebuild / exact-difference removal (ShelfDelta)
- [customers] Customer card pickup off card display slots: CardShelfSync host-authoritative snapshot diff (CardShelfDelta) - card slots deplete on the guest when host customers buy
- [customers] Customer queueing at registers: host-only sim; queue/counter positions ride NpcSync puppet transforms; register cart state mirrored via RegisterState + RegisterMirror
- [customers] Customer checkout / being served: RegisterServe lets the guest work the register (ServeRequest->host runs the NPC-cashier automation; ScanEcho/ServeStatus/RegisterState back to client). Sale money/XP flow through the shared econ mirror
- [customers] Customer REVIEWS & reputation (all review types incl. SmellyCustomer, ItemPrice, ItemVariety, TournamentProblem, StoreGeneric): ReportSync host-authoritative - reviews stream as they land (AddCustomerReview postfix nudge + hash gate), sequence-numbered tail dedup, m_CustomerReviewCount/ScoreAverage mirrored, list capped at 50 (Sync/ReportSync.cs)
- [customers] End-of-day report counters driven by customers (customerVisited/customerBoughtItem/customerBoughtCard/customerPlayed/customerDisatisfied/smellyCustomerCleaned): ReportSync WriteState/ClientApplyState full snapshot
- [customers] Trade / sell-in walk-up customers: TradeServe (Sync/TradeServe.cs) - host pre-rolls offer via CustomerTradeData persistence slot, TradeState->client, guest answers via native screen (TradeOp), money/cards run once host-side through vanilla OnPressAccept; both-players-on-one-customer guard via _guestClaims; the '!' exclaim mesh mirrored via NpcSync Exclaim flag
- [customers] Customers at play tables (match visuals): PlayTableSync (Sync/PlayTableSync.cs) mirrors per-seat TableGameItemSet playmat/deckbox/comic visuals host->client; the card fan/single on the customer rides NpcSync's IsPlaying flag. m_TotalPlaytableCustomerCount etc. are host-only sim counters
- [customers] Tournament customers (bracket/pairing/prizes/sign-ups): host-only sim; TournamentSync mirrors m_TournamentData + per-customer digest to drive the pairing screen; tournament customers rendered as NpcSync puppets; guest scheduling blocked (accepted host-only decision)
- [customers] Smelly customer VISUAL + AutoCleanser STATION cleaning: smelly stink FX mirrored via NpcSync Smelly flag; the auto-cleanser station's spray/clean runs host-only against the real customer list and mirrors the cleaned result back, with on/off/refill forwarded via ContainerSync (OpCleanserToggle/OpCleanserRefill)
- [customers] No shoplifter / theft / security-camera-vs-customer mechanic exists in the game (grep of Customer.cs for steal/thief/security found only price-praise speech strings) - nothing to sync
- [customers] Special customer behaviors (chatty, looking-for-card-only, exact-change, target-buy-item lists, max-money scaling): all host-only sim state set in Customer.ActivateCustomer (blocked on the guest); their observable outcomes (shelf depletion, sales, reviews, puppet motion) are already mirrored by the engines above
- [workers] Hiring: HireWorkerPanelUI.OnPressHireButton HirePrefix blocks the client BEFORE it charges (level/dup/affordability re-checked locally) and forwards StaffOp OpHire; host HostHire runs the full vanilla hire path exactly once (PriceChangeManager.AddTransaction + CEventPlayer_ReduceCoin + SetIsWorkerHired + ActivateWorker + report counters + AchievementManager.OnStaffHired) with a double-hire guard on GetIsWorkerHired and an affordability re-check (StaffSync.cs:131-256).
- [workers] Hire fee wallet charge: charged host-side only; client path blocked before ReduceCoin so the shared wallet is never double-charged (StaffSync.cs:158-162, 240).
- [workers] Give-bonus and Fire and task-assignment screens: reachable only via Worker.OnMousePress on a live real Worker (WorkerInteractUIScreen.OpenScreen called from Worker.OnMousePress, Worker.cs:233-251); on the guest real workers are blocked from activating (WorkerManager.ActivateWorker ClientBlockPrefix, GamePatches.cs:31-32) and any stray active worker is deactivated every tick (CoopCore NpcSweepTick, CoopCore.cs:916-920), and puppets are stripped customer-prefab clones with colliders/Worker-script removed (NpcSync.Spawn, NpcSync.cs:579-597). Screens are physically unreachable, so their shared-wallet mutations (OnPressGiveBonus ReduceCoin, hire) cannot fire from the guest - accepted as designed (StaffSync.cs:9-24).
- [workers] Worker roster mirror: host broadcasts hired roster + per-worker save-data essentials (tasks, bonus count, price/pack settings) hash-gated on change + 15s heal (StaffState); client applies m_IsWorkerHired and m_WorkerSaveDataList WITHOUT calling ActivateWorker, so the guest's phone hire screen and salary-derived numbers read the truth (StaffSync.cs:258-395).
- [workers] Wages / salary bill: GetTotalSalaryCost is host-authoritative (WorkerManager.EvaluateSalaryCost sums costPerDay over CPlayerData.GetIsWorkerHired). Bill accrual (RentBillScreen.EvaluateNewDayBill) blocked on client (ShopStateSync BillAccrualPrefix) and mirrored via ShopState; PaySalaryBill blocked-before-charge and forwarded as ShopOp EBillType.Employee (ShopStateSync.cs:131-132,195,353; RentBillScreen.cs:105-108,216-233). No double charge, salary owned by host.
- [workers] Worker assignment (register/restock/cleaning/card-restock/pack-opener/set-price): only settable through the unreachable WorkerInteractUIScreen on the guest; host-side assignments run in the host's worker sim and the resulting task settings mirror to the guest read-only via StaffState (task bytes + PackTypes list). No guest op needed (StaffSync.cs Entry/Collect 73-322).
- [workers] Worker AI acting on shared shelves/cards/registers: workers simulate host-only; their mutations to item shelves, card display slots, and register checkouts are host-authoritative and reach the guest through the existing ShelfDelta/CardShelfDelta/RegisterState/NpcState streams. Guest sees results, not a divergent local sim (WorkerManager ActivateWorker blocked + NpcSweep; NpcSync streams worker puppets at 8Hz).
- [workers] Worker-held boxes hidden on guest: when a HOST worker carries a box, BoxSync.Snapshot marks it Carried via IsBeingHeld(box) so guest mirrors hide it and never position-stomp it out of the worker's hands (BoxSync.cs:58-60,206-209,536,584). The host->guest direction of the worker/box interplay is correctly covered.
- [workers] Female worker puppet gender: worker names aren't 'Female'-prefixed, so NpcSync ships an explicit Female flag (w.m_IsFemale) so female workers render from the correct prefab on the guest (NpcSync.cs:143-147,335,528-529).
- [workers] Day-start worker respawn: WorkerManager.OnDayStarted -> ActivateWorker is blocked on the client (ClientBlockPrefix), so hired workers never spawn a live/clickable real Worker on the guest at day rollover (WorkerManager.cs:189-206; GamePatches.cs:31-32).
- [register] Guest works the register (scan): CoopCore ServeRequest handler (3346-3358) -> RegisterServe.Serve ScanningItem branch calls scan.OnMouseButtonUp() / card.OnMouseButtonUp() on the HOST, driving the exact vanilla NPC-cashier automation; sale money/XP flow through the shared economy host-side.
- [register] Scan display mirror: ScanEcho (28) carries (counterIdx, isCard, price=after-before delta, authoritative host running total, identity) -> RegisterServe.ApplyScanEcho SETS the guest counter's m_TotalScannedItemCost to hostTotal-price then re-adds, so the checkout bar is pinned to host truth and cannot drift/accumulate (CoopCore 3379-3400).
- [register] Take payment: RegisterServe.Serve TakingCash branch fires customer.m_CustomerCash.OnMouseButtonUp() host-side -> Customer.OnCashTaken -> StartGivingChange + state=GivingChange, all host-authoritative.
- [register] Cash change (two-phase): GivingChange branch calls NPCEvaluateMoneyChange()+CheckChangeReady() then OnPressSpaceBar() on the next press, identical to worker automation; AddCoin(net) broadcast via CoinSet (RegisterServe.cs 165-176 vs InteractableCashierCounter.Update worker branch 190-206).
- [register] Card payment: GivingChange card branch invokes EvaluateCreditCard(totalCost) host-side; because m_IsMannedByPlayer is false the credit-card UI setup is correctly skipped and OnPressSpaceBar completes the sale + AddCoin(totalCost) (RegisterServe.cs 156-164, verified against InteractableCashierCounter.EvaluateCreditCard 426-443 and OnPressSpaceBar 366-377).
- [register] Sale money reaches guest wallet: OnPressSpaceBar CEventPlayer_AddCoin -> HostTick CoinSet mirror (MsgType 6) per the wallet coverage area.
- [register] Register visual mirror + serve prompt: RegisterState (27) host->client 2Hz CollectStates/ReadStates -> RegisterMirror.Apply renders the current customer's unscanned cart items at their real counter-local positions and drives PromptFor (CoopCore RegisterCollectTick 970-980, RegisterMirrorTick 871-891).
- [register] Click-to-scan natural register: guest left-click raycast on a mirrored cart prop -> ServeRequest for that counter; click during TakingCash/GivingChange (IsPaymentPhase) -> ServeRequest to advance the sale (CoopCore 1747-1771, RegisterMirror.TryGetPropCounter/IsPaymentPhase).
- [register] Serve key: tap V = one action, hold V = ~4/s auto-serve, both gated by _serveThrottle and text-focus guards; sends ServeRequest to host connId 1 (CoopCore 1717-1745).
- [register] Multi-counter identity: ServeRequest/RegisterState key on the counter's index in ShelfManager.m_CashierCounterList, and PopulationSync (kind 4) keeps that list byte-for-byte order-identical across host+guest via the game's own SpawnInteractableObject list-order recipe + OnDestroyed reconciliation, so mid-session-placed counters stay index-aligned (PopulationSync.cs 24-45, GetList case 4).
- [register] Host-manning guard: RegisterServe.Serve returns 'the host is already at this register' when m_IsMannedByPlayer is true, so a guest ServeRequest cannot fight a host manual sale; a worker on the counter is intentionally allowed (vanilla calls arbitrate) (RegisterServe.cs 80-84).
- [register] No-customer / not-ready guards: Serve returns early on null/inactive customer and on non-serve states (RegisterServe.cs 85-87, default branch 178-180); guest ServeRequest at a counter with a live trade offer is suppressed so it can't stomp the trade prompt (CoopCore 1728-1731).
- [register] Sale-complete cleanup: ServeStatus 'sale complete!' triggers RegisterServe.ClientResetScreens + ClientResetTotals across EVERY counter so a multi-counter guest's scanned-item bar can't carry to the next customer (CoopCore 3368-3376).
- [register] Cashier counter checkout/trade toggles: SettingsSync CashierPostfix on SetCanCheckout/SetCanTradeCard forwards per-counter toggles (SettingsSync.cs 432-434, 510).
- [register] Customer queue at the register: queue is pure host-side customer sim; queued customers render as NpcSync puppets (visual only), and CustomerManager.Update/ActivateCustomer are ClientBlockPrefix'd so the guest never originates queue behavior.
- [register] No separate cash-register balance and no self-checkout: CashierCounterSaveData holds only pos/rot/box/isDisableCheckout/isDisableTrading (no per-till cash float); grep for Self.?Checkout/AutoCheckout across decompiled found nothing, so the shared wallet is the only balance and it is host-authoritative + CoinSet-mirrored.
- [register] Mid-sale host walk-up race: if the host mans a counter mid guest-sale, OnMouseButtonUp sets m_IsMannedByPlayer=true and the guest's next ServeRequest is refused; if the host finishes the sale manually one frame before the guest's ServeRequest lands, the state is Idle/next-customer and Serve returns early -> no double AddCoin (verified Serve guards + OnPressSpaceBar single-shot m_IsChangeReady gate).
- [register] Mid-sale guest disconnect: host keeps the partial-checkout customer and finishes it manually or via worker (host-authoritative); host disconnect leaves the guest frozen in the borrowed world with saves blocked (knownAccepted).
- [time-save] Day/night clock mirror: HostTick broadcasts DayTime (day,hour,min) every 2s on the RELIABLE channel (CoopCore.cs:2280-2296); case DayTime (2607) mirrors m_CurrentDay + pins clock, runs LightManager.DelayUpdateEnv only on dayChanged&&InGameLevel and lets exactly one OnDayStarted through via AllowNextDayStarted (GamePatches.cs:375,383-387). Reliable channel means the day-change packet can't be dropped.
- [time-save] Day-boundary join: m_CurrentDay and m_LightTimeData are in the save (CGameData.cs:242,20; PropagateLoadData 444,868) and transferred at join; the first DayTime after join carries the same day, so dayChanged==false and NO spurious day-change coroutine fires. Guest joining mid-day-end is covered by the save + report openScreen sync.
- [time-save] Guest day cannot end/advance locally: DayEndBlockPrefix (GamePatches.cs:377) blocks CEventPlayer_OnDayEnded and OnDayStarted on the client (except the one allowed via AllowNextDayStarted). LightManager pins m_HasDayEnded=false on the client (CoopCore.cs:2647) so LightManager.EvaluateTimeClock never self-fires day-end.
- [time-save] Sleep / skip-night is host-only: GoNextDay (LightManager.cs:348 -> m_CurrentDay++) is reachable only via EndOfDayReportScreen.OnPressGoNextDay, which ReportSync.NextDayBlockPrefix blocks on the client (ReportSync.cs:180). The hold-to-skip Update() path in EndOfDayReportScreen calls OnPressGoNextButton, which ReportSync.NextButtonPrefix reroutes to CloseScreen (no day advance, no phantom game-event fee) on the client (ReportSync.cs:160-178). Guest also can't even OPEN the report via keypress because InteractionPlayerController gates ShowGoNextDayScreen on GetHasDayEnded() which the mod pins false.
- [time-save] End-of-day report on both sides: host ReportOpenedPostfix snapshots m_GameReportDataCollect at open and ReportSync.HostTick ships it immediately with openScreen=1 (ReportSync.cs:134-205); client ClientApplyInner replaces its report with the host's authoritative numbers and TryOpenReportScreen opens the same popup, guarding against phone/cash-mode conflicts (ReportSync.cs:379-420). Reviews append by sequence number.
- [time-save] Autosave + all save paths blocked for guest: every persistence path funnels through CGameManager.SaveGameData - autosave loop, OnApplicationQuit (line 374), OnApplicationPause/SteamDeck (384), EndOfDayReportScreen.OpenScreen (65), PauseScreen.OnPressBackMenu (94), SaveLoadGameSlotSelectScreen.OnPressSaveGame (157) - and SaveGuardPrefix (GamePatches.cs:355-365) blocks all of them when Role==Client OR GuestBorrowedWorld.
- [time-save] GuestBorrowedWorld lifecycle: set on Join/JoinSteam (CoopCore.cs:390,1155), persists through mid-session host-disconnect (Shutdown only clears it if !InGameLevel, CoopCore.cs:1647), and cleared only once safely back at title (Update: Role==None && !InGameLevel, CoopCore.cs:1698). Clean 'Back to Menu' loads Title scene -> OnSceneLoaded Shutdown('left the session') for the client (CoopCore.cs:754-758) -> guard clears at title. This half is solid; the GAP is only the non-Title Load-Game scene change.
- [time-save] Level-up: ProgressSet (case at CoopCore.cs:2655) sets m_ShopLevel FIRST then queues SetShopExp/SetFame, and queues CEventPlayer_ShopLeveledUp only when level>prevLevel - avoiding spurious client-side level-ups. The level-up banner (LevelUpNotificationUI) and unlock-notification text are purely cosmetic; licenses/rooms are NOT auto-unlocked at level-up in this game (they require a separate purchase, synced via LicenseUnlock/ShopOp), so no divergence from the mirrored level.
- [time-save] Time-of-day lighting heal: LightState (5s change-gate, case at CoopCore.cs:~3210) applies full LightTimeData with a drift>600 rollover skip and a 10s guard after a day-mirror to avoid racing DelayUpdateEnv, plus surgical shop-light-bit apply (CoopCore.cs:3234-3255). Prevents the guest sky/clock from freezing at day-end or fighting the day-change coroutine.
- [time-save] Local screenshot/photo saving (CGameManager.TakeScreenshot / ScreenCapture.CaptureScreenshot, lines 264/315) writes to the user's Documents/Screenshots folder - local-only, never touches the save slot, so no guest save-pollution concern (cosmetic/local by design).
- [restock-storage] Guest restock ORDERS: GamePatches.OrderPrefix blocks the client's local RestockManager.SpawnPackageBoxItemMultipleFrame phantom and forwards ForwardOrder->OrderRequest with (itemType,isBig,name FNV) identity; host ResolveRestockIndex tiered-match then RestockManager.SpawnPackageBoxItemMultipleFrame host-side; miss => refund via CEventPlayer_AddCoin + Toast (CoopCore.cs:2912-2955). Delivery boxes mirror back via BoxSync.
- [restock-storage] Restock drip-spawner cap (m_RestockSpawnBoxLimit): a guest order at/over cap is NOT lost - SpawnPackageBoxItemMultipleFrame always appends to m_SpawnBoxItemWaitingList + m_SpawnBoxRestockIndexWaitingList/m_SpawnBoxItemCountWaitingList (RestockManager.cs:333-342) and drips as boxes clear (Update gate line 79); the waiting queue is host save-persisted. The TooManyBox popup is a harmless host-only local UI (guest never sees a phantom).
- [restock-storage] Client OOB/warehouse-lock box teleport sweep suppressed: GamePatches.RestockUpdatePrefix pins m_OutofBoundCheckTimer to 0 on the client so RestockManager.Update's random-reposition sweep (lines 102-129) never fires against host-authoritative box positions.
- [restock-storage] Client card-base market-price roll blocked: GamePatches.GenerateCardMarketPriceBlockPrefix blocks RestockManager.GenerateCardMarketPrice on the client so the guest doesn't roll divergent bases (host bases come via the join save). [Noted as knownAccepted/1.0.28 fix - not re-reported as a gap.]
- [restock-storage] Warehouse rack stored-box CONTENTS mid-session: BoxSync Stored entries carry StoreShelf/StoreComp + Count; ApplyClosedCount rebuilds the compartment pos list when needed so counts converge (BoxSync.cs:231-262). WorldSync deliberately skips kind-1 racks (WorldSync.cs:86-91) because their compartment count is a stored-BOX tally, not loose items - BoxSync owns racks.
- [restock-storage] Warehouse rack ADD/REMOVE renumbering: WarehouseShelf index is a live positional index recomputed by ShelfManager.InitWarehouseShelf/RemoveWarehouseShelf (decompiled 1296-1318). Because the host re-broadcasts every stored box's StoreShelf on the 1.5s BoxState after any rack add/remove, and PopulationSync (kind 1) reconciles the guest's m_WarehouseShelfList to the host's roster order using the game's own spawn recipe (append order matches), host and guest re-converge on matching indices at rest. Transient churn during a mid-list rack removal self-heals; the B2 TryEvictGhostAndRetryStore path (BoxSync.cs:344-413) plus store give-up/pin handle rejected stores without a revert-war.
- [restock-storage] Guest STORING a box on a warehouse rack: BoxSync.ClientTick detects m_IsStored true and reports Stored+StoreShelf/StoreComp (908-916 via Snapshot); HostApplyRequest->ApplyToBox stored branch runs the game's DispenseItem restore recipe host-side (924-1021).
- [restock-storage] Trashing a loose box: InteractableTrashBin.DiscardBox->box.OnDestroyed()->GamePatches.BoxDestroyedPrefix->BoxSync.LocalBoxDestroyed->NotifyLocalDestroyed->MsgType.BoxRemoved; host HostApplyRemoval destroys the real box with UnhookIfStored + flood-guard (BoxSync.cs:579-594, CoopCore.cs:2975-2985).
- [restock-storage] Box carried in hands (player OR worker): FiBeingHold/m_IsBeingHold guard + IsLocallyCarried keep a held box from being position-stomped or rebuilt, both host (HostApplyRequest 536, HostApplyRemoval 584) and client (ClientApplyInner 717,745,795); hold-box soft-lock recovery via CoopCore.ForceExitHoldBox when a held box is consumed host-side (BoxSync.cs:798-802).
- [restock-storage] Empty-box storage COUNT and store-back: ContainerSync mirrors GetBoxStoredCount, TakeBoxPrefix/StoreBoxPrefix+Postfix forward take/store ops, RequestBoxResync->BoxSync.ForceBroadcastNextTick pushes the freshly spawned/consumed box promptly (ContainerSync.cs:492-510,807-928). [Container-station domain, verified for the BoxSync interplay.]
- [restock-storage] 1000-box wire cap + absent-id sweep: cap raised to 1000 (ushort wire), guest skips the absent-box destroy sweep on a truncated (>=1000) snapshot so past-cap boxes aren't falsely destroyed (BoxSync.cs:485-521,778-813). [1.0.28-era fix - not re-reported.]
- [restock-storage] Dispensing box items ONTO a shelf and emptying a box to 0: DispenseItem (decompiled 216-267) moves items box->shelf without changing the box's m_ItemType, so BoxSync's type guard passes; shelf side syncs via WorldSync, box count via BoxSync. Both directions converge.
- [grading-mail] Guest submit (enroll): GradingSync.SubmitPrefix blocks vanilla OnPressSubmitButton BEFORE the fee charges, forwards GradingOp; host HostApplyOp replicates the vanilla submit body (PriceChangeManager.AddTransaction, supplyCost, CEventPlayer_ReduceCoin, enroll in m_GradeCardInProgressList) — anchor GradingSync.cs 129-262, 337-455
- [grading-mail] Guest-side grading maturation is blocked: GradingSync.MatureBlockPrefix on RestockManager.OnDayStarted returns false when Role==Client OR GuestBorrowedWorld, so the mirrored pending list never self-matures/rolls grades/spawns a phantom box — verified RestockManager.OnDayStarted (decompiled 533-619) is pure grading maturation; anchor GradingSync.cs 117-127
- [grading-mail] Pending-submission list mirrored to guest: HostTick broadcasts GradingState (1.5s, hash-gated at game-hour granularity, 15s heal); client ClientApplyState adopts m_GradeCardInProgressList wholesale and repaints an open GradeCardWebsiteUIScreen — anchor GradingSync.cs 285-303, 462-515
- [grading-mail] Guest grading UI reads the mirror truth: GradeCardWebsiteUIScreen (new-submission 4-cap, set panels) and read-only GradedCardSetCheckStatusScreen both read m_GradeCardInProgressList, which the mirror keeps authoritative — decompiled GradeCardWebsiteUIScreen.cs / GradedCardSetCheckStatusScreen.cs (no mutation paths, no cancel/withdraw button)
- [grading-mail] Days-left display fix: client clamps mirrored m_DayPassed to [0, m_ServiceDays-1] so the un-clamped vanilla (m_ServiceDays - m_DayPassed) render never shows '-1 day' on the guest who doesn't run maturation — anchor GradingSync.cs 488-494
- [grading-mail] m_MinutePassed advancement on guest is harmless: LightManager.EvaluateTimeClock increments m_MinutePassed on the mirrored list as the guest advances its mirrored clock (decompiled LightManager.cs 478-481), but the guest's list is overwritten wholesale by GradingState each game hour and maturation is blocked, so no divergence
- [grading-mail] Binder accounting on submit is not double-counted: ungraded cards leave the binder at SELECTION time via CPlayerData.ReduceCard (CardDelta mirror), so HostApplyOp deliberately does NOT reduce the binder again on enroll — decompiled CollectionBinderFlipAnimCtrl.cs 189/230; GradingSync.cs comment 305-311
- [grading-mail] GO fee integrity: guest forwards the real on-screen bill (m_ServiceTotalCost, GO's per-value price) instead of the vanilla-flat guess; host sanity-gates clientFee (finite/non-negative/affordable) and returns cards on rejection — anchor GradingSync.cs 176-233, 337-419 (this is the 1.0.28 grading-fee fix, confirmed intact)
- [grading-mail] GO extra service tiers: serviceLevel widened 0..255 on both wire ends; GO's GetGradeCardServiceData prefix clamps out-of-range levels and the GO-absent flat recompute path guards its raw GetGradeCardServiceData index — anchor GradingSync.cs 228, 347, 388-407
- [grading-mail] Submit rejection (slot/wallet race) returns cards, no loss: HostApplyOp reject check (412-419) precedes the degrade loop, and ReturnRejectedCards AddCards each card back (grade>10 Remembered for GO so it re-files into the graded album) via a single host AddCard whose CardDelta broadcast reaches every peer — anchor GradingSync.cs 326-335, 411-419
- [grading-mail] Graded RETURN boxes mirrored: CardBoxSync host broadcasts m_CardPackagingBoxList by index (1.5s, hash-gated, 10s heal); client reconciles via RestockManager.SpawnPackageBoxCard with the exact broadcast CardData (grades pre-rolled host-side, never re-rolled) and despawns via OnDestroyed — anchor CardBoxSync.cs 197-231, 359-442; decompiled RestockManager.SpawnPackageBoxCard 375-456
- [grading-mail] Guest collect of a graded box: OpenBoxPrefix blocks vanilla OnPressOpenBox (which would AddHoldCard->store->AddCard locally = duplicate) BEFORE any handoff and forwards OpCollect (identity-hashed); host AddCards each stored card (mirrored to both binders via CardDelta), increments gemMint report + runs the two achievement checks, then despawns its real box — anchor CardBoxSync.cs 161-167, 277-312; replicates decompiled InteractablePackagingBox_Card.OnPressOpenBox 94-123
- [grading-mail] Collect duplication guarded: client sets _recentlyCollected[cardsHash] so a stale pre-collect snapshot can't resurrect the just-collected box for a ghost double-collect; local reveal is anim+SFX+toast only with no local AddCard — anchor CardBoxSync.cs 368-393, 519-558
- [grading-mail] Graded card carried/moved boxes: client reports carried transitions + settled positions (OpReport), host hides world copy incl. Card3dUIGroup followers while a peer carries; index/identity reconciliation via FindBox with cardsHash fallback — anchor CardBoxSync.cs 249-268, 447-513, 634-678
- [grading-mail] Graded-box removal & reload-flood defense: guest OpRemoved forwards local box death; host FindBox + OnDestroyed; BoxSync.RemovalFlooded shared budget prevents a reloading client's box-list teardown from mass-destroying real boxes — anchor CardBoxSync.cs 314-330, 582-609
- [grading-mail] Divergent gradedCardIndex between host/guest is not a removal hazard for MIRRORED ops: AddCard recomputes gradedCardIndex = list.Count+1 locally on each side (decompiled CPlayerData.cs 1499-1511), but every mirrored graded removal (CardDelta graded branch + GradedRemove) uses RemoveGradedCard(ignoreGradedCardIndex:true) which matches by identity, not index — anchor CoopCore.cs 507-527, GradingSync/CardBoxSync host collect
- [grading-mail] Encoded (>10) graded grades: refused when Grading Overhaul absent (ApplyCardDelta 489-493), Remembered/BindCert'd before AddCard when present so GO's anti-cheat doesn't re-encode as fake — anchor CoopCore.cs 499-505; matches knownAccepted GO-both-sides assumption
- [grading-mail] Workers do not auto-collect graded return boxes (grep of decompiled WorkerManager.cs for CardPackagingBox/OpenBox/GradeCard = no matches), so collection is a player-only action fully covered by the host-authoritative + guest-forward model; no unhandled host-only automation path
- [catch-all] Tutorial / task progression — host-authoritative snapshot pushed to guest via ShopState (WriteState appends m_TutorialIndex + m_TutorialDataList; ClientApplyInner/ApplyTutorial replays it, resetting each subgroup and re-feeding values once). Re-broadcast is gated on tutorial-progress change (hash includes tut index + per-task condition/value). All 15 ETutorialTaskCondition credit paths (RestockItem, PickupBox, PutItemOnShelf, SetItemPrice, OpenShop, CheckoutCustomer, OpenPack, BuyPlayTable, CustomerPlay, BuyCardShelf, PutCardOnShelf, SellCard, UnlockBasicCardBox, InteractCashierCounter) fire host-side because the underlying mutations forward to the host (ShelfRequest/CardShelfRequest/OrderRequest/ItemPriceContrib/ShopOp sign/ServeRequest/ContainerOp/FurnitureOrder/LicenseUnlock). Guest's own local tutorial credit is overwritten by the host snapshot — host is authoritative. anchor: ShopStateSync.cs WriteState/ApplyTutorial lines 296-582, CoopCore.cs:1406/3055 UnlockBasicCardBox credit
- [catch-all] Guest day-advance is gated shut — LightManager.m_HasDayEnded is pinned false on the guest (CoopCore DayTime handler, FiHasDayEnded?.SetValue(...,false) 'never freeze at closing', line 2647), so InteractionPlayerController's GoNextDay key path (decompiled line 393, gated on GetHasDayEnded()) never fires for the guest. Day rollover is host-only; EndOfDayReportScreen.OnPressGoNextDay is additionally NextDayBlockPrefix'd (ReportSync). anchor: CoopCore.cs:2647, ReportSync NextDayBlockPrefix
- [catch-all] Phone / tablet apps are a pure VIEW — every confirm path routes to a covered forwarded op: bills/rooms/signs->ShopOp, hire->StaffOp, deco buy/game-event/counter toggles->SettingsOp, grading website->GradingOp, tournament schedule->host-only block+toast, restock/furniture->OrderRequest/FurnitureOrder, price->ItemPriceContrib. PhoneManager itself (decompiled) only lerps the phone mesh and opens screens; no direct shared-state mutation. anchor: PhoneManager.cs, SettingsSync/ShopStateSync/StaffSync/GradingSync patch prefixes
- [catch-all] Grade-card WEBSITE screen (GradeCardWebsiteUIScreen) only navigates; the actual charge/enroll is GradedCardSubmitSelectScreen.OnPressSubmitButton, covered by GradingSync SubmitPrefix (block-before-charge -> GradingOp). anchor: GradingSync.ApplyPatches SubmitPrefix
- [catch-all] Shop billboard / rename — InteractableShopBillboard.OnMouseButtonUp just opens ShopRenamer.ShowRenameShopScreen, which is RenamerBlockPrefix'd on the guest; shop name itself syncs host->client via ShopName (37). No separate marketing/advertising system exists in this build. anchor: GamePatches.RenamerBlockPrefix, HostTick ShopName
- [catch-all] Camera / photo mode — CameraPhoneScreen/CameraPhotoGalleryScreen/CameraPhoneModeUIScreen have zero shared side effects (no coin/card/fame/achievement/CEvent calls); the photo gallery is per-client local per the inventory. Cosmetic/local. anchor: decompiled camera screens (grep for shared mutators returns empty)
- [catch-all] Music / audio — SoundManager volumes are per-client cosmetic (knownAccepted local-only); no jukebox/radio shared-state system exists. anchor: inventory 'Other/cosmetic', SoundManager
- [catch-all] Trash bin (InteractableTrashBin.DiscardBox) — routes to packagingBox.OnDestroyed(), which is covered per box type: item boxes via BoxDestroyedPrefix/BoxRemoved (BoxSync), furniture boxes via FurnBoxSync DestroyedPrefix, card boxes via CardBoxSync DestroyedPrefix. Guest trash forwards as BoxRemoved (42). anchor: GamePatches.BoxDestroyedPrefix, FurnBoxSync/CardBoxSync DestroyedPrefix
- [catch-all] PvP deck-play (PlayCardGameManager) — the manager is a fully STUBBED no-op class (OpenDeckListScreen/OnCloseDeckListScreen/UpdateSelectedCardData all empty); the deck feature is inactive in this build. m_DeckCompactCardDataList is save-data transferred once at join; no active mini-game mutates it mid-session. Not-present / bucket 4. anchor: decompiled PlayCardGameManager.cs (18 lines, all empty bodies)
- [catch-all] Achievements — Steam-only cosmetic. UnlockAchievement sets AchievementData.IsAchieved+Store() (Steam) and m_IsAchievementUnlocked (save flag); nothing in-game gates or rewards on it (no coin/content unlock). The mod already fires per-account achievement hooks on the guest for actions it participates in (ContainerSync OnCardPackOpened, CardBoxSync OnCheckGemMint/CollectedGradedCardSet, StaffSync OnStaffHired, CoopCore OnItemLicenseUnlocked). Per-Steam-account and harmless to diverge. Cosmetic. anchor: AchievementManager.cs UnlockAchievement body, mod On* calls
- [catch-all] Guest quit-to-menu cleanly leaves the session — OnSceneLoaded('Title' && Role==Client) -> Shutdown('left the session'); mid-session host loss -> guest gets 'keep walking around, nothing touches your saves' and Shutdown('host connection lost') with GuestBorrowedWorld persisting so saves stay blocked (knownAccepted). Pause never freezes the shared world (PauseNoFreezePostfix forces timeScale=1; mod ticks in its own Update independent of game pause). anchor: CoopCore.cs OnSceneLoaded:754, Disconnect drain:1811-1816, GamePatches.PauseNoFreezePostfix
- [catch-all] Wall/floor/ceiling deco (equipped indices A/B) syncs via SettingsOp OpEquipDeco (six ints) + SettingsState; placed physical deco OBJECTS covered by PopulationSync kind 5 (m_DecoObjectList, identity via m_DecoObjectType, SpawnDecoObject) and ObjMoveSync for moves; deco BUY blocked-before-charge. anchor: SettingsSync OpEquipDeco/BuyDecoPrefix, PopulationSync case 5
- [catch-all] Keybinds / language / FOV / collection-sorting / audio settings — per-client local by design (knownAccepted). SettingScreen changes never mutate shared shop state. anchor: inventory 'Other/cosmetic'
