# .NET build output, anywhere in the tree.
**/bin/
**/obj/
decompiled/
Directory.Build.user.props
backup/
diag/
# dist/ holds personal deployment kits and release packaging - releases ship
# via GitHub Releases and Nexus, not the source tree
dist/
self-hosted-runner/game/
self-hosted-runner/steam/
self-hosted-runner/.env
tools/coop-vanilla-profile.ps1
decompiled-grading/
# Machine-local per-repo notes (nested AGENTS.md is loaded by OpenCode on exploration,
# but these hold this PC's paths and must never be committed).
src/CardShopCoop/AGENTS.md
